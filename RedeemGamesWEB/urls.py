from django.conf.urls import include, url
from . import views

urlpatterns = [
    #url(r'^$', views.post),
   	url(r'^$', views.jogo),
    #url(r'^$', views.jogochave),
    #url(r'^$', views.lista)
]


# Register your models here.
